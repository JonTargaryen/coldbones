{
  "name": "coldbones",
  "private": true,
  "description": "ColdBones — visual analysis platform (monorepo root)",
  "scripts": {
    "test": "npm run test:frontend && npm run test:backend && npm run test:infra",
    "test:frontend": "cd frontend && npx vitest run",
    "test:backend": "cd backend && python -m pytest --tb=short -q",
    "test:infra": "cd infrastructure && npx jest --passWithNoTests",
    "lint": "npm run lint:ts && npm run lint:py",
    "lint:ts": "cd frontend && npx tsc --noEmit",
    "lint:py": "ruff check backend/ lambdas/ worker/ && ruff format --check backend/ lambdas/ worker/"
  }
}
